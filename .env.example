# Optional server-side AI research (OpenAI-compatible chat completions endpoint).
# Keep secrets out of source. Configure hosted values through Sites.
RESEARCH_API_URL=
RESEARCH_MODEL=
RESEARCH_API_KEY=
